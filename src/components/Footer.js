import React from "react";
import { NavLink } from "react-router-dom";

const Footer = () => (
  <footer className="footer">
    <div>This is the footer</div>
    <div>Footer looks good</div>
  </footer>
);

export default Footer;
